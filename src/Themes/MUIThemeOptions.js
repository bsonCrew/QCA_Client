import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1A374D',
      dark: '#00acc1',
      light: '#80deea',
      blue: '#013764',
      red: '#E4012E',
      grey: '#575757',
    },
    secondary: {
      main: '#E33E7F',
    },
  },
});

export default theme;
